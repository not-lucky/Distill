# `compile` package API

The `compile` package contains the Anki deck compilation logic that runs
as a Python subprocess from the Node.js orchestrator. The package lives
at `src/compile/` and is re-exported from the backward-compatible
`src/compile.py` module.

::: compile
options:
show*source: true
show_root_heading: true
show_root_full_path: false
members_order: source
separate_signature: true
docstring_section_style: table
show_symbol_type_heading: true
show_symbol_type_toc: true
filters: ["!^*"]
